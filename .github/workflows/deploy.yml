name: Build & Deploy Web Game

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install pygbag
        run: python3 -m pip install pygbag pygame-ce

      - name: Build game
        run: |
          - name: Build game
  run: |
    python3 -m pygbag webapp --build --package mordor-rain-web --title "Mordor Rain"

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web